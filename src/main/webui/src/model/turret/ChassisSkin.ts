export default class ChassisSkin {
    name: string;
    label: string;
    icon: string;
    constructor(name: string, label: string, icon: string) {
        this.name = name;
        this.label = label;
        this.icon = icon;
    }
}